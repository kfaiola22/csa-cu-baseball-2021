#for INDOOR SCRIMMAGES first (fact check against google data visual) -> then new data set
#read in data

#get vector of unique names

#create data frame of missing values

#fill in a name column using unique names

#loop for each name

  #index the rows with data

